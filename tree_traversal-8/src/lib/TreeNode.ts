class TreeNode {
    key: number;
    left: number;
    right: number;

    constructor(key: number, left: number, right: number) {
        this.key = key;
        this.left = left;
        this.right = right;
    }
}

export default TreeNode;